model User {
  id       Int    @id @default(autoincrement())
  nom      String
  prenom   String
  pseudo   String
  email    String @unique
  password String
  role     String

  posts   Post[]
  Account Account?

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@map("user")
}
